import React from "react";
import PromoBottomDefault from "@/components/(promotions-bottom)/promo-bottom-default";

const PromoBottom = ({ banner }: any) => {
  return <PromoBottomDefault banner={banner} />;
};

export default PromoBottom;
